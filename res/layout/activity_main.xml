<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black"
    tools:context="com.yarik.wenor.MainActivity" >

    <ListView
        android:id="@+id/storiesListView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:divider="@android:color/white"
        android:dividerHeight="3dp">
    </ListView>
    
    <ScrollView 
        android:id="@+id/articlesFulltextScrollView"
        android:layout_width="match_parent"
        android:layout_height="@dimen/articles_fulltext_scrollview_height"
        android:layout_centerInParent="true"
        android:fillViewport="true"
        android:visibility="gone">
        
        <TextView
            android:id="@+id/articlesFulltextTextView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:layout_gravity="center"/>"
    </ScrollView>

</RelativeLayout>
